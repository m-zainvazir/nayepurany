# nayepurany
A FastAPI application that serves a simple HTML interface and uses cookies to track unique user chat histories.  Uses Shopify integration to run smoothly over the website. The application is hosted on a remote server and managed via PuTTY, using Uvicorn to handle web traffic.
